
name: Ranorex Run

on: 
  workflow_dispatch:

jobs:
  build:

    runs-on: [self-hosted, Windows]


    steps:

      - uses: actions/checkout@v3

      - name: run
        run: D:\Atlas-Automation-main\AtlasAutomation\bin\Debug\UAT1.exe
        

        
